/*
	Problem description:

	Given set of coins (denominations) and target amount,
	return the minimum amount of coins needed to reach amount
	using set of coins.
*/

const changeMaking = (denominations, amount) => {

	return 0;
};

export default changeMaking;